var rek = require('rekuire');
var paymaya = rek('index');

// TODO: create test for webhooks
// webhooks collide in names but are queried using IDs
xdescribe('Webhooks', () => {
	it('allows creation of webhooks', () => {

	});
});

